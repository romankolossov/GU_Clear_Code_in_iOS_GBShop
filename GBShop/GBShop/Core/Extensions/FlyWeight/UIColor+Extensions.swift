//
//  UIColor+Extensions.swift
//  GBShop
//
//  Created by Roman Kolosov on 10.03.2021.
//

import UIKit

extension UIColor {

    // for TabBarControlle

    static let tabBarBackgroundColor: UIColor = .init(white: 0.1, alpha: 1.0)
    static let tabBarTintColor: UIColor = .systemPurple

    // for NavigationControllers

    static let  navigationBarBackgroundColor: UIColor = .init(red: 71 / 255, green: 21 / 255, blue: 111 / 255, alpha: 1.0)
    static let  navigationBarTitleTextForegroundColor: UIColor = .white

    // for root view in root VC of NavigationControllers

    static let rootViewBackgroundColor: UIColor = .init(red: 201 / 255, green: 33 / 255, blue: 168 / 255, alpha: 1.0)

    // for UserData

    static let userDataTextColor: UIColor = .darkGray
    static let userViewBackgroundColor: UIColor = .white
}
